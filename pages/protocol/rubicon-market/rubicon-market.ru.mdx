---
title: Rubicon | Рынок Rubicon
pageTitle: Рынок Rubicon
description: Обзор и документация по контрактам и заказам в Rubicon
---

Основная логика обмена Rubicon находится в [RubiconMarket](https://github.com/RubiconDeFi/rubicon_protocol/blob/master/contracts/RubiconMarket.sol) смарт-контракт — центральная книга заказов Rubicon. Полезно думать об этом смарт-контракте как о среднем уровне, который облегчает одноранговый обмен цифровыми активами через книгу заказов.

# 📊 Рынок Рубикон

## Обзор

Смарт-контракт Rubicon Market реализует книги заказов в сети и механизм сопоставления для одноранговой торговли [ERC-20 tokens](https://ethereum.org/en/developers/docs/standards/tokens/erc-20/).

Книга ордеров — это список ордеров на покупку и продажу актива, отсортированных по цене. Этот контракт реализует каждую книгу заказов ERC20/ERC20 в виде двух отсортированных сдвоенных списков, один для стороны покупки и один для стороны продажи данного рынка.

Контракт использует модель условного депонирования ликвидности; когда в книге размещается лимитный ордер, эти токены отправляются в контракт. Если/когда ордер выполнен, контракт напрямую сопоставляется с трейдерами, и токены отправляются каждой стороне. Ордер может быть отменен, и контракт вернет токены отправителю.

Rubicon Market является производным продуктом MakerDAO с открытым исходным кодом [OasisDEX](https://oasisdex.com/) и наследует лицензию AGPL3.

## Торговые функции

### предложение()

```
    function offer(
        uint256 pay_amt,
        ERC20 pay_gem,
        uint256 buy_amt,
        ERC20 buy_gem,
        uint256 pos,
    ) public returns (uint256)
```

| Parameter Name         | Type        | Description                                                                                                                       |
| ---------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| pay\_amt               | uint256     | Quantity of ERC-20 tokens the maker is selling                                                                                    |
| pay\_gem               | address     | ERC-20 token the maker is selling                                                                                                 |
| uint buy\_amt          | uint256     | Quantity of ERC-20 tokens the maker is buying                                                                                     |
| ERC20 buy\_gem         | address     | ERC-20 token the maker is buying                                                                                                  |
| uint pos               | uint256     | Position in the sorted list to place the order. Set this to 0 unless you know the exact position (closest ID) to insert the order |
| \[Optional] matching   | bool        | Optional boolean to determine whether the offer should be automatically matched (placed in the sorted list)                       |

The primary function for placing limit orders on Rubicon. The **pay_amt** quantity of the **pay_gem** token will be sent to the order book contract, sitting in escrow until it is filled or canceled. Always set pos to **0** unless you know the exact position in the sorted lost to insert the order.

### cancel()

```
    function cancel(uint id)
        public
        can_cancel(id)
        returns (bool success)
```

| Parameter Name         | Type        | Description                                    |
| ---------------------- | ----------- | ---------------------------------------------- |
| id                     | uint256     | The id of the order the user wants to cancel   |

Эта функция используется для заполнения или «выбора вишен» определенного заказа в книге. Вызывающий абонент оплачивает комиссию тейкера. Кроме того, вы можете использовать функцию маршрутизации **take()** для достижения того же результата и заполнения определенного предложения.

### buyAllAmount()

```
     function buyAllAmount(
        ERC20 buy_gem,
        uint256 buy_amt,
        ERC20 pay_gem,
        uint256 max_fill_amount
    ) external returns (uint256 fill_amt)
```

| Parameter Name         | Type        | Description                                    |
| ---------------------- | ----------- | ---------------------------------------------- |
| buy\_gem               | address     | ERC-20 token the taker is buying               |
| buy\_amt               | uint256     | Quantity of tokens the taker is buying         |
| pay\_gem               | address     | ERC-20 token the taker is selling              |
| max\_fill\_amount      | uint256     | Maximum amount of pay tokens sold              |

Попытки обменять **sell_amt** количество токенов **sell_gem** как минимум на **min_fill_amount** количество токенов **pay_gem**. Транзакция будет отменена, если трейдер получит меньше указанной минимальной суммы. Это ордер на продажу «исполни или убей».

## Просмотр функций

### getBestOffer()

```
function getBestOffer(ERC20 pay_gem, ERC20 buy_gem)
    public
    view
    returns (uint256)
```

Возвращает идентификатор предложения в верхней части стакана.

Бывший. Вызов этой функции с WETH как **pay_gem** и USDC как **buy_gem** вернет лучший спрос на WETH/USDC. Переключение токенов вернет лучшую ставку.

### getWorseOffer()

```
function getWorseOffer(uint256 id)
    public
    view
    returns (uint256)
```

Возвращает следующее худшее предложение в отсортированном списке. Худшее предложение — это более высокое, если целевой ордер — аск, и более низкое, если это бид. В обоих случаях он вернет более новый, если они равны.

### getBuyAmount()

```
    function getBuyAmount(
        ERC20 buy_gem,
        ERC20 pay_gem,
        uint256 pay_amt
    ) external view returns (uint256 fill_amt)
```

Возвращает количество полученных токенов **buy_gem**, если было потрачено указанное количество токенов **pay_gem**. Используется для проверки текущего состояния книги ордеров.

### getPayAmount()

```
function getPayAmount(
        ERC20 pay_gem,
        ERC20 buy_gem,
        uint256 buy_amt
    ) external view returns (uint256 fill_amt)
```

Возвращает количество токенов **pay_gem**, необходимое для покупки указанного количества токенов **buy_gem**. Используется для проверки текущего состояния книги ордеров.

### getOfferCount()

```
function getOfferCount(ERC20 sell_gem, ERC20 buy_gem)
        public
        view
        returns (uint256)
```

Возвращает количество предложений в стакане для указанной пары.

### получитьFeeBPS()

```
function getFeeBPS()
    public
    view
    returns (uint256)
```

Возвращает комиссию получателя протокола в базисных пунктах.